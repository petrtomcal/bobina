{{product.name}}<br/>
{{product.price|number_to_usd}}
{{product.size}}
<br />
<div class="sub-list">
<table class="common-table" cellpadding="0" cellspacing="0">
  <thead>
   <tr>    
      <th colspan="3">&nbsp;</th>
   </tr>
  </thead>
{% for attachment in attachments %}
  <tr>    
    <td>{{ attachment.name | truncate: 50 }}</td>
    <td>{{ attachment.size | size_to_bytes }}</td>    
  </tr>
{% endfor %}    
</table>
</div>
<a href="/products/index">Back to shop</a>