<div class="editation_menu"> 
  <%= link_to 'New user', :action => 'new' %>
</div>
<div class="sub-list">
<table class="common-table" cellpadding="0" cellspacing="0">
  <thead>
   <tr>    
<!--    <th>< pof_order_header(@pof, :order_by => 'first_name', :label => 'Fisrt name', :title => 'Click to order') %></th>
    <th>< pof_order_header(@pof, :order_by => 'last_name', :label => 'Příjmení', :title => 'Click to order') %></th>-->
      <th colspan="6">&nbsp;</th>
   </tr>
  </thead>
<% even = 0
  for user in @users 
  even+=1 -%>
  <tr id="<%= user.id %>" <%= "class=\"even\"" if (even % 2)==0 %>>
    <td><%= link_to user.email, { :controller => 'users', :action => 'show', :id => user.id  } %></td>
    <td><%= user.first_name %></td>
    <td><%= user.last_name %></td>
    <td><%= link_to 'Show', {:action => 'show',:id => user.id} %></td>
    <td><%= link_to 'Edit', {:action => 'edit', :id => user.id} %></td>
    <td><%= link_to 'Destroy', {:action => 'destroy',:id => user.id}, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>

</table>

<br />