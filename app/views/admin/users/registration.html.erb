<% form_for(@user, :url => { :action => "create_registration" }, :html => { :class => 'common-form'}) do |f| %>
  <%= render :partial => 'form' %>
  <p>
    <%= f.submit 'Save' %>
  </p>
<% end %>

<%= link_to 'Back', :action => 'index' %>