<% form_for(@category, :url => { :action => "update", :id => @category.id }, :html => { :class => 'common-form'}) do |f| %>  
  <%= render :partial => 'form' %>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', {:action => 'show',:id => @category.id} %> |
<%= link_to 'Back', :action => 'index' %>