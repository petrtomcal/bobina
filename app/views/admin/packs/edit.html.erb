<% form_for(@pack, :url => { :action => "update", :id => @pack.id }, :html => { :class => 'common-form'}) do |f| %>
  <%= render :partial => 'form' %>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', {:action => 'show',:id => @pack.id} %> |
<%= link_to 'Back', :action => 'index' %>